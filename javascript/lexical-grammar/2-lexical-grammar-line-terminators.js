// This function does not provide line terminators such as
// a carriage return \r or line feed \n
// Without the line terminators, it reports a syntax error.
function main() {
    const message = "Hello world!!!" return message
}

// Another option is to use semi-colon to explicitly indicate the end of each statement.

function anotherMain() {
  const message = "Hello world!!!";return message;
}

const resultMain = main()

const resultAnotherMain = anotherMain()

resultMain

resultAnotherMain
